# AWS Cognito Configuration
VITE_COGNITO_POOL_ID=ap-southeast-1_Gnh61YBRT
VITE_COGNITO_CLIENT_ID=11mm0p6vsgmp41bq4fenmql52r
VITE_AWS_REGION=ap-southeast-1

# Public API Gateway (AWS ELB → nginx ingress → path-based routing)
# All services are routed through a single ELB endpoint.
# The nginx ingress dispatches to the correct microservice by path prefix.
VITE_API_BASE_URL=https://d2i2xj8fuzhl98.cloudfront.net/api/users
